# dbt with Databricks adapter
dbt-core>=1.7.0,<2.0.0
dbt-databricks>=1.7.0,<2.0.0

# Databricks SDK and CLI
databricks-sdk>=0.20.0
databricks-cli>=0.18.0

# Azure SDK (for ADLS / Key Vault integration)
azure-identity>=1.15.0
azure-storage-file-datalake>=12.14.0
azure-keyvault-secrets>=4.7.0

# PySpark (local testing; Databricks runtime provides its own)
pyspark>=3.5.0

# Delta Lake (local testing)
delta-spark>=3.0.0

# Testing & code quality
pytest>=7.4.0
pytest-mock>=3.12.0
sqlfluff>=3.0.0
sqlfluff-templater-dbt>=3.0.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
